# Contributing

The canonical contribution guide lives in `docs/contributor/contributing.md`.

Use that document for:

- contribution workflow
- quality checks before opening a change
- links to ADRs, testing strategy, and release notes

Quick checks before opening a change:

```bash
pnpm lint
pnpm --filter web check-types
```
