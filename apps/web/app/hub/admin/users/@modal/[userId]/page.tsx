// Keep this route as an explicit no-op for the @modal slot.
// User open-badge management now lives on its own page instead of a modal.
export default function AdminUsersOpenBadgesModalPage() {
  return null;
}
