# Prisma (local dev)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"
SHADOW_DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB_shadow?schema=public"

# Security (used by app)
SECRET_PEPPER=change_me
